// Preview pages removed - no longer needed without template system
// Kept as empty exports to avoid import errors during transition

const PreviewTemplate = () => null;
const PreviewUserPage = () => null;

export { PreviewTemplate, PreviewUserPage };
